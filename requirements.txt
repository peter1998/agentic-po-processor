# Core
fastapi==0.115.0
uvicorn[standard]==0.30.0
python-multipart==0.0.32  # required by FastAPI for multipart/form-data (file uploads)

# Agent framework (verified: latest is 1.2.x as of Jul 2026, not 0.2.x)
langgraph==1.2.6
langchain==1.3.14
langchain-anthropic==1.4.0

# LLM / Vision (verified: latest is 0.116.x as of Jul 2026)
anthropic==0.116.0

# Embeddings (verified: latest is 0.4.x, not 1.0.0)
voyageai==0.4.1

# Vector DB (verified: latest is 1.5.x major version, not 0.5.x)
chromadb==1.5.9

# File parsing
pdfplumber==0.11.4
pdf2image==1.17.0
pandas==2.2.3

# Dev/demo tooling (used to generate the sample PDF in data/demo_files/, not needed at runtime)
reportlab==4.2.5

# Database
sqlalchemy==2.0.36

# Utils
python-dotenv==1.0.1
pydantic==2.9.2
pydantic-settings==2.6.0
pytest==8.3.2
pytest-asyncio==0.24.0
pytest-cov==5.0.0